# 待回复问题清单

> 把答案填在每条问题的 `**答：**` 后面即可。
> 标 ⚠️ 的会影响动笔，标 💬 的写代码前答就行。

---

## A. ⚠️ 影响动笔，必须先答

### A1. `autotreehole.cn/daily` 的部署形态

独立站点部署，还是既有 AutoTreehole 站点的子路径？

- 选项 A：独立站点（一个 repo、一个 nginx location、一套独立后端）
- 选项 B：AutoTreehole 的子路径 / 子模块（共享 repo / 共享后端 / 共享用户体系）

**答：**
B。autotreehole.cn本来是另一个项目，不要动它，我们只做一个子路径，网站通过子路径访问。

---

### A2. 服务器环境状态

服务器上是否已装 Python 3.11 / nginx / SQLite CLI / Node？

- 如果没装，我会用 apt + pip 全装
- 如果已装部分，我会先核对版本

**答：**
有些装了，你自己开发时候连上去看

---

### A3. SSH 私钥处理

密钥文件里写的是 Windows 路径 `C:\Users\Gao runyu\.ssh\treehole_key`，我所在环境是 Linux 本机。

- 选项 A：你在本机已生成的私钥直接发我（base64 / 文件）
- 选项 B：我自己生成新 keypair，把公钥给你，你贴到服务器的 `~/.ssh/authorized_keys`
- 选项 C：服务器只允许密码登录（不安全，不推荐）

**答：**已经放在wsl linux系统下。"\\wsl.localhost\Ubuntu-22.04\home\groy\.ssh\treehole_key"

---

## B. 💬 写代码前答即可（不急）

### B1. GitHub repo 名称

默认计划：`gry1024/daily`。

- 选项 A：保持 `gry1024/daily`
- 选项 B：改成 `gry1024/autotreehole-daily`
- 选项 C：改成 `gry1024/<其它>`：__________

**答：** 
A

---

### B2. School 模块首页主视图

- 选项 A：顶部"未来 7 天活动"通栏 + 下方"近期通知"列表（推荐，跟"信科生提前看日程"匹配）
- 选项 B：顶部"今日通知"列表 + 下方"未来 7 天活动"

**答：**
分为“今日”栏目和各个单独的功能栏目。一个功能模块就要单独一个栏目。“今日”栏目是只加载最新找到的包括了各个功能栏目在最新一轮搜索新增的内容

---

### B3. 邮件摘要策略

每天 6:30 是否发邮件摘要？

- 选项 A：每天一封，包含 9 个模块各 3 条精华（推荐）
- 选项 B：只在有"重要羊毛过期前 3 天"或"重要校内活动前 1 天"时推
- 选项 C：暂不做邮件摘要，纯靠打开站点

**答：**
每天凌晨搜索，然后8：00发一封，有内容就发摘要，没有或者故障，警告，搜索失败等也要通过邮件告诉我原因。

---

### B4. 设备使用频次

你日常用手机还是电脑看这个站点？

- 选项 A：电脑为主（手机能用就行，侧边栏不激进折叠）
- 选项 B：手机为主（激进折叠 + 顶部 tab 横滑）
- 选项 C：差不多

**答：**
都要做，移动端电脑端的ui布局都要做好。
---

### B5. 历史归档保留时长

- 新闻类（AI News / Finance News / School）：建议 90 天
- 论文 / 量化题库 / LeetCode / 羊毛：永久
- GitHub Trending：永久
- 行情：永久

- 选项 A：保持以上建议
- 选项 B：自定义：新闻 ___ 天 / 羊毛 ___ 天 / 其它：__________

**答：**
你自己定

---

### B6. School 模块"量化"白名单

量化相关搜索关键词是否要按你心里的具体公司/社团名单收敛？

例子：
- 公司：九坤、幻方、明汯、衍复、锐天、致诚卓远、灵均、量化明珠
- 社团：北大 Quant Society、北大金融数学与金融工程、北大数量经济与数理金融
- 公众号：__________

**答：**
只需要按量化头部颈部公司来，中国或者外国（optiver，jane street等）都可以
---

### B7. 现有 AutoTreehole 项目（如果有）

设计系统文档里提到"本设计系统抽离自 AutoTreehole 2026 年 7-8 月迭代"，意味着已有 AutoTreehole 项目。

- 选项 A：它就是 autotreehole.cn 的根，我做的 /daily 是它的子路径
- 选项 B：它是另一个独立项目（autotreehole.cn 上的另一个站），与 /daily 完全独立
- 选项 C：没有 AutoTreehole 这个项目，文档只是参考

**答：**
是另一个项目，完全无关本项目，不要动那边的代码和服务。我们项目只是借用一个子路径作为访问入口，仅此而已
---

### B8. LLM 调用成本 / 频次控制

每个模块每次抓取都会调 minimax-m3，9 个模块一天约消耗 200-500 次 LLM 调用。

- 选项 A：照计划跑（不限制）
- 选项 B：保守跑，AI News / Arxiv / Deals 等"量大"模块改为每条只调一次、不重试
- 选项 C：自定义上限：每天最多 ___ 次

**答：**
A，无限量

---

### B9. 数据失败容错

某模块当日抓取失败时（例如 arxiv 接口挂了）：

- 选项 A：前端显示"今日数据缺失"，cron 不重试
- 选项 B：cron 自动重试 3 次（间隔 10 分钟），失败再放弃
- 选项 C：自动 fallback 到备用数据源（如 arxiv 失败改用 OpenReview RSS）

**答：**
B

---

### B10. 是否需要登录保护

站点在公网，是否需要基础访问控制？

- 选项 A：完全公开（任何人都能看）
- 选项 B：加一个共享密码（cookie 存 token，前端输入一次）
- 选项 C：限制 IP 白名单（只你的 IP 可访问）

**答：**
A

---

## C. ✅ 无需回答（计划中的默认假设）

下面这些我在计划里已有默认方案，如果你不同意再告诉我，否则就照默认走：

| 项 | 默认 |
|---|---|
| 数据库 | SQLite 单文件 |
| 缓存层 | 无（每天 cron 一次，前端直接读 DB） |
| 前端框架 | 原生 HTML/CSS/JS，不引 Vue/React |
| 静态托管 | nginx 直接 serve，不走 Node |
| 进程管理 | PM2（管 FastAPI） |
| HTTPS | Let's Encrypt 自动续期 |
| 时区 | Asia/Shanghai |
| 日志保留 | 14 天 |
| DB 备份 | 每天 03:00 备份到 `/home/groy/daily/backups/`，保留 30 天 |
| 主题切换 | 默认亮色，支持暗色模式，默认跟随系统 |
| 侧边栏顺序 | 1.Quant 2.AI News 3.Arxiv 4.School 5.LC 6.Eng 7.Fin 8.GitHub 9.Deals（可拖拽改顺序） |
| RSS 输出 | 第一版不做 |
| PWA | 第一版不做 |

如果你要改某一项，直接在下面写：

**修改项：**
无
---

*填完回复我即可开工。*
